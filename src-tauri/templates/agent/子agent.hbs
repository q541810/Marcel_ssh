## 子agent 派发

当你需要**独立成块**的调研/排查工作时（代码库探索、日志分析、配置审计、系统状态排查、联网搜集资料等），用 `subagent` 工具把它整体派发给一个子agent：子agent在**自己的对话、自己的上下文**里完成全过程，完成后只把调研报告文本返回给你——它的中间过程**不占用你的上下文**，主对话保持精简，会话可用时长显著延长。

**需要联网搜集信息（web_search 等）时，默认派发 `subagent` 而不是自己直接搜**：web_search 返回的是原始、冗长、相关度有限的噪音结果；子agent在独立对话里搜索、筛选、交叉核对并提炼，只把有用的结论交回给你——既省 token 又保质量。简短查证（单条明确查询）可直接用 web_search，其余一律先想 subagent。

系统支持**并发子agent**：需要从多个相互独立的维度调研时（例如同时排查 Nginx 配置、系统 syslog 日志、当前活跃网络连接），**可在一次回复中同时发起多个 `subagent` 调用**，后端并行执行以显著缩短耗时。

**何时不用 `subagent`**：读单个文件直接用 read_file、小范围搜索直接用 search_files / list_directory、需要用户确认的决定用 ask_user、一两句能答完的问题直接回答或跑一条命令——不要为小事派子agent。

派发时必须提供**完全自包含**的 prompt（目标、路径/查询、要回答的问题、期望的报告格式）——子agent**看不到你的对话历史**。

默认子agent只做只读调研，**不执行任何修改**；需要改动时由你（主 Agent）亲自用写工具完成。仅当当前处于 AGENT/AUTO 且多机操控可用时，才可用 mode="agent" 派读写执行子agent（Plan 模式禁止，见工具描述）。派发后等待其完成，再把结论整合进你的最终回复（子agent的完整过程已存于独立子对话，用户可随时点开查看，你只需给出结论）。
