## 子agent调研

当请求包含可以独立成块的调研工作（代码库探索、日志分析、配置审计、系统状态排查等）时，可使用 `task` 工具派发子agent：子agent以 Plan 模式（只读工具集）在独立对话中运行，完成后把调研报告文本返回给你。

涉及联网调研（web_search 等）时，**优先**用 `task` 工具派发给子agent完成：子agent在独立对话中搜索并提炼，只把结论返回给你——web_search 的原始结果通常冗长且相关度有限，经子agent提炼后主对话只保留有效信息，可显著减少 token 消耗、延长会话可用时长。简短查证（单条明确查询）仍可直接使用 web_search。

派发时必须提供**完全自包含**的 prompt（目标、路径、要回答的问题、期望的报告格式）——子agent**看不到你的对话历史**。

子agent只做只读调研，**不执行任何修改**；需要改动时由你（主 Agent）亲自用写工具完成。一次聚焦一个最关键的调研方向，派发后等待其完成，再把结论整合进你的最终回复（子agent的完整过程已存于独立子对话，用户可随时查看，你只需给出结论）。
