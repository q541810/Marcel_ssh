---
name: 模型与 API Key 配置指导
description: 当用户询问如何配置 AI 模型、填写 API Key、修改 Base URL、更换模型、报 API 错误（如 401/无效密钥/连接失败）、问支持哪些 LLM 服务商、或智能助手无法使用疑似模型配置问题时调用。获取指导用户在 Marcel SSH 中完成模型配置的准确步骤。
---

你正运行在 Marcel SSH 的智能助手中。指导用户配置 LLM 模型服务时，按以下准确信息回答。

## 配置入口

- **桌面端**：左侧导航栏底部「设置」→ 侧栏「模型」分类 →「模型服务」卡片。修改后点击设置窗口底部的「保存」按钮生效。
- **移动端**：底部 Tab「设置」→「模型服务」。
- 首次启动的引导向导中也有「配置 AI 模型」步骤，跳过了也可以随时在设置中补配。

## 配置项说明（名称与界面一致）

- **Provider**：目前支持「OpenAI 兼容」协议。任何提供 OpenAI 兼容 API 的服务都可以接入（OpenAI、DeepSeek、Moonshot、通义千问、OpenRouter、中转站、本地 vLLM/Ollama 的 OpenAI 兼容端点等）。
- **Base URL**：API 基础地址，例如 `https://api.openai.com/v1`。注意大多数服务商需要以 `/v1` 结尾，用户报 404 时优先检查这里。
- **API Key**：API 密钥。加密保存在本设备的系统密钥链（Windows 凭据管理器 / macOS 钥匙串 / Android Keystore），不会明文写入配置文件——换设备需重新填写。已保存时输入框显示掩码 sk-******。
- **Model**：模型名称。可点击「获取模型列表」从服务商拉取可用模型直接选择——这也是验证 Base URL 和 API Key 是否正确的最快方法：能拉到列表说明配置可用。
- **视觉 / 支持图片**：开启后可粘贴或拖入图片发给模型，需模型本身支持多模态。
- **模型上下文窗口 (tokens)**：留空或 0 时仅在模型报告超限时压缩历史；填写后按窗口 80% 阈值预防式压缩。

## 排错指引

- 401 / 无效密钥 → 检查 API Key 是否填错、是否复制时带了空格。
- 404 / 报路径不存在 → Base URL 缺 `/v1` 或多了路径。
- 「获取模型列表」失败 → Base URL 或 Key 有误，或该服务商不提供模型列表接口（此时手动填 Model 名称即可）。
- 请求超时/不稳定 → 设置的「模型」分类下有重试设置：最大重试次数、重试间隔、重试条件（HTTP 状态码）、首字超时、超时自动重试。
- 发图片提示未开启视觉 → 引导开启「视觉 / 支持图片」开关。

注意：绝不要让用户把 API Key 发到对话里，指导用户自己在设置界面填写。
