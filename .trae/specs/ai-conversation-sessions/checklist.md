- [x] 后端 `AgentConversation` 结构体定义完整，包含 id, sessionId, title, createdAt, updatedAt 字段
- [x] SQLite 数据库初始化成功，`conversations` 和 `messages` 表创建正确
- [x] 所有 5 个新增 Tauri commands 实现并注册
- [x] `AppState` 中包含 `conversation_db` 字段
- [x] `Cargo.toml` 已添加 `rusqlite` 依赖
- [x] 前端 `AgentConversation` 类型已定义
- [x] 前端 IPC 封装包含所有新增 commands
- [x] `agentStore.ts` 已重构为按会话管理消息
- [x] AgentPanel 头部右侧显示"新建会话"和"历史会话"按钮
- [x] 历史会话抽屉可正常打开、关闭、列出会话、切换会话
- [x] 断开 SSH 连接时，前端清理对应 AI 会话的内存数据
- [x] 删除 SSH 保存连接配置不影响已存在的 AI 会话数据
- [x] `cargo check` 编译通过
- [x] `pnpm tsc --noEmit` 编译通过
