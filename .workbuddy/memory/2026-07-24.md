# 2026-07-24 工作日志

## 自动同步功能全面代码审查
- 受用户委托对 Marcel SSH 项目做全面审查，重点为自动同步功能与服务端 `server/`（Python FastAPI 同步服务端）。
- 服务端 10 个 .py 文件逐行阅读；客户端 Rust 引擎与前端由专项探索代理梳理。
- 产出报告：`D:\my\Marcel SSH\review-auto-sync.md`

### 关键发现（摘要）
- P0：S-F2 每日清理会删除仍有 sync_snapshots 的账户（移除全部设备即丢数据）；S-F4+C-F2 传输层未强制 TLS。
- P1：C-F1 push 失败无重试；S-F1 账户删除双因子名存实亡（account_id==config_code_hash）；S-F3 WS 保活依赖文本 ping；C-F3 冲突明文进日志；FE-F1/2/3 前端超时/竞态/状态不同步。
- 服务端亮点：零知识 E2E 加密、参数化 SQL、ASGI chunked body 限制、限流、Admin 硬化均良好。

### 项目结构备注
- 自动同步三端：Rust `src-tauri/src/sync/` + 前端 `src/stores/syncStore.ts` 等 + Python `server/`。
- 服务端为"带版本号的 LWW KV 存储"，三方合并完全在客户端 `merge.rs` 完成。

## 维护者反馈与修订（同日）
用户就审查报告提出反馈，已采纳并改代码/文档：
- **S-F4/C-F2（TLS 强制）**：降级为「设计确认/不适用」——个人部署场景 TLS 由用户或反代自管，不强制。移出 P0。
- **S-F2（每日清理）**：降级为「设计确认」——零知识模型本地为数据真相源，清理仅设备数=0 时触发且定在凌晨 04:00，非数据丢失。移出 P0。
- **S-F1（双因子）**：已修正 `server/main.py` `delete_account` docstring/注释（双重验证→一致性校验，明确 API Key=完全账户访问权）。
- **S-F5（限流）**：已改 `server/rate_limit.py`：`/api/account/setup` 限流 (5,60)→(3,60)；`join` 不创账户维持 5/60s。
- 当前唯一 P0 遗留：C-F1（客户端 push 失败无自动重试）。
- 报告 `review-auto-sync.md` 已同步更新（结论表/3.8/第7节/总体评价/新增第9节）。

## 引导流程重做：同步账户门（同日，经用户纠偏后收敛）
- **需求**：所有引导开始之前先问用户有没有同步账户；恢复成功直接跳过正常引导。同时重做引导界面（尤其移动端）。
- **重要纠偏**：第一版我做了渐变按钮/光晕/渐变文字/ob-rise 错落动画/修饰文案（"开始之前"等），用户明确否决：**要简洁、干净、克制、现代化，尊重原有设计风格与代码结构，不要 AI 味的随意发挥和不自然文案**。已全盘回退视觉系统（删除 obShared.tsx 与 globals.css 新增动画），最终方案严格使用原设计语言。
- **最终结构**：两端 onboarding 三相位 `gate → restore → steps`，样式全部沿用原有组件与配色（zinc 卡片、indigo-600 按钮、原进度圈/dots、原文案）。gate 问"你有同步账户吗？"，两个选项卡用原特性卡样式；restore 内联 pairJoin → `waitForInitialSyncPull` 等首轮 pull → idle 1.8s 自动进入，timeout/error 手动进入。
- **保留的新增**（均属功能非装饰）：`src/components/onboarding/SyncRestore.tsx`（SyncRestoreFlow 状态机，样式对齐 MobileSyncPairPage）；移动端 backHandler 接入（restore/steps 可返回 gate）；`src/lib/waitForInitialSync.ts`（此前 untracked 的预埋工具，本次首次被消费）。
- **关键事实**：`hasCompletedOnboarding` / `hasAcceptedSyncDisclaimer` 在 `sync/profile.rs` 映射为 `None`（不同步）；提交加入即写入 `hasAcceptedSyncDisclaimer: true`，避免设置页再弹免责声明。
- **验证**：tsc/eslint 全过。
- **环境注意**：本机沙箱拦截 vite build 清空 `dist/`（safe-delete trash 失败），验证构建用 `--outDir dist-verify` 绕过；vitest 有 3 个既有套件加载失败（`self is not defined`，@xterm/addon-fit UMD），与改动无关。
